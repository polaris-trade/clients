# This file is generated by `cargo hakari`.
# To regenerate, run:
#     cargo hakari generate

[package]
name = "workspace-hack"
version = "0.1.0"
edition = "2021"
description = "workspace-hack package, managed by hakari"
# You can choose to publish this crate: see https://docs.rs/cargo-hakari/latest/cargo_hakari/publishing.
publish = false

# The parts of the file between the BEGIN HAKARI SECTION and END HAKARI SECTION comments
# are managed by hakari.

### BEGIN HAKARI SECTION
[dependencies]
observability-core = { git = "https://github.com/polaris-trade/observability", tag = "observability-core-v0.2.0", default-features = false, features = ["flush-tokio"] }
serde_core = { version = "1", default-features = false, features = ["alloc", "result", "std"] }
toml_datetime = { version = "1", features = ["serde"] }
toml_parser = { version = "1" }
transport_core = { git = "https://github.com/polaris-trade/transports", tag = "transport_tokio-v0.4.1", features = ["observability"] }
winnow = { version = "1" }

[build-dependencies]
serde_core = { version = "1", default-features = false, features = ["alloc", "result", "std"] }
toml_datetime = { version = "1", features = ["serde"] }
toml_parser = { version = "1" }
winnow = { version = "1" }

### END HAKARI SECTION
