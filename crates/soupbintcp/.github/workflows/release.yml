name: release

on:
  push:
    branches: [main]

jobs:
  release:
    uses: 0x67/ci/.github/workflows/release.yml@main
    with:
      publish: false
    secrets:
      release-plz-token:    ${{ secrets.RELEASE_PLZ_TOKEN }}
      cargo-registry-token: ${{ secrets.CARGO_REGISTRY_TOKEN }}
